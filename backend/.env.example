# RAG Platform Backend Environment Variables
# Copy this file to .env and configure as needed
# The run.sh script will automatically populate these with correct paths

# MongoDB Configuration
MONGO_URL="mongodb://localhost:27017"
DB_NAME="rag_platform"

# CORS Configuration
CORS_ORIGINS="*"

# Cache Directories - IMPORTANT: Keep everything within project directory for portability
# These will be automatically set by run.sh to use project-relative paths
# HF_HOME="PROJECT_DIR/.cache/huggingface"
# TRANSFORMERS_CACHE="PROJECT_DIR/.cache/huggingface"
# SENTENCE_TRANSFORMERS_HOME="PROJECT_DIR/.cache/sentence_transformers"

# Optional: Gemini API Key (can also be set via Settings page)
# GEMINI_API_KEY="your-api-key-here"
